# 💻 My System Configuration (Dotfiles)

This repository contains my personal and professional development environment settings. It is designed to be portable across machines using symbolic links.

## 📂 Repository Structure
- `nvim/`: Neovim configuration (Lua based)
- `shell/`: Bash/Zsh aliases and functions
- `git/`: Global gitconfig and ignore patterns
- `scripts/`: Automation and installation scripts

## 🛠️ Installation

1. **Clone the repo:**
   ```bash
   git clone [https://github.com/wfmarcelo/dotfiles.git](https://github.com/wfmarcelo/dotfiles.git) ~/dotfiles

2. **Run the Setup:**

In the repository root folder:

```Bash
chmod +x install.sh
./install.sh

