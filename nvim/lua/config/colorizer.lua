require("colorizer").setup({
  user_default_options = {
    names = false, -- Don't highlight words like "Blue"
    mode = "background", -- Highlight the hex code background
    tailwind = true, -- Enable tailwind colors
  }
})
